@model MapBul.Web.Models.CategoriesModel
<div class="ibox">
    <div class="ibox-title">
        <h3>Редактировать категорию</h3>
    </div>
    <div class="ibox-content">
        <div class="form-group">
            @Html.DropDownList("EditCategorySelect", (Model.Categories).Select(c => new SelectListItem { Value = c.Id.ToString(), Text = c.Name }), new Dictionary<string, object> { { "id", "EditCategorySelect" }, { "data-placeholder", "Выберите категорию" }, { "class", "form-control chosenselect" } })
        </div>
        <div class="form-group">
            <input id="EditCategoryButton" type="button" class="btn btn-primary" value="Редактировать" />
        </div>
    </div>
</div>

@using MapBul.Web.Helpers
@model MapBul.Web.Models.CategoriesModel
<div class="col-md-6">
    <div class="ibox">
        <div class="ibox-title">
            <h3>Структура категорий</h3>
        </div>
        <div class="ibox-content">
            <div class="dd" id="nestable">
                <ol class="dd-list">
                    @foreach (var category in Model.Categories.Where(c => c.ParentId == null))
                    {
                        <li class="dd-item" data-id="@category.Id">
                            <div data-id="@category.Id" class="dd-handle EditCategoryButton"><img style="width: 30px;" src="@Html.MapImage(category.Icon)" /> @category.Name </div>
                            @if (category.category1 != null && category.category1.Count != 0)
                            {
                                <ol class="dd-list">
                                    @Html.Partial("Partial/_CategoriesBranchPartial", category.category1)
                                </ol>
                            }
                        </li>
                    }
                </ol>
            </div>
        </div>
    </div>
</div>

<div class="col-md-3" id="NewCategoryContainer">
    @Html.Action("_NewCategoryPartial")
</div>

<div class="col-md-3" id="EditCategoryContainer">
    @Html.Partial("Partial/_EditCategoryPartial",Model)
</div>



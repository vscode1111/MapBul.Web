@model MapBul.Web.Models.CitiesModel
<div class="col-md-6">
    <div class="ibox">
        <div class="ibox-title">
            <h3>Страны</h3>
        </div>
        <div class="ibox-content">
            <table class="table table-responsive CitiesTable">
                <thead>
                <tr>
                    <th>Страна</th>
                    <th></th>
                </tr>
                </thead>
                <tbody>
                @foreach (var country in Model.Countries)
                {
                    <tr>
                        <td>@country.Name</td>
                        <td>
                            <input data-id="@country.Id" type="button" class="DeleteCountryButton btn btn-danger" value="Удалить" />
                        </td>
                    </tr>

                }
                </tbody>
            </table>
            <div class="row">
                <div class="col-md-6 form-group">
                    <input class="form-control" id="NewCountryInput" type="text" placeholder="Новая страна" />
                </div>
                <div class="col-md-10 form-group">
                    <input type="button" class="btn-primary btn" data-actionurl="@Url.Action("AddCountry")" value="Добавить" id="NewCountryButton" />
                </div>
            </div>
        </div>
    </div>
</div>
<div class="col-md-6">
    <div class="ibox">
        <div class="ibox-title">

            <h3>Города</h3>
        </div>
        <div class="ibox-content">
            <table class="table table-responsive CitiesTable">
                <thead>
                <tr>
                    <th>Город</th>
                    <!--<th>Регион</th>-->
                    <th>Страна</th>
                    <th></th>
                </tr>
                </thead>
                <tbody>
                @foreach (var city in Model.Cities)
                {
                    <tr>
                        <td>@city.Name</td>
                        <!--<td>city.region.Name</td>-->
                        <td>@city.country.Name</td>
                        <td>
                            <input data-id="@city.Id" type="button" class="DeleteCityButton btn btn-danger" value="Удалить" />
                        </td>
                    </tr>
                }
                </tbody>
            </table>
            <div class="row">
                <div class="form-group col-md-6">
                    <select class="form-control" id="CountrySelectForCity">
                        @foreach (var country in Model.Countries)
                        {
                            <option data-countryname="@country.Name" data-countrycode="@country.Code" data-placeid="@country.PlaceId" value="@country.Id">@country.Name</option>
                        }
                    </select>
                </div>
                <div class="col-md-6 form-group">
                    <input class="form-control" id="NewCityInput" placeholder="Новый город" />
                </div>
            </div>
            <div class="row">
                <div class="col-md-12 form-group">
                    <input type="button" class="btn-primary btn" data-actionurl="@Url.Action("AddCity")" value="Добавить" id="NewCityButton"/>
                </div>
            </div>
        </div>
    </div>
</div>